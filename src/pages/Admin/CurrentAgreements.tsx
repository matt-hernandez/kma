import React from 'react';
import { addPageData } from '../../util/add-page-data';

const slug = '/agreements/current';
const title = 'Current Agreements';

export default addPageData(() => {
  return <div />
}, { slug, title });
