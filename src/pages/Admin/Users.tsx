import React from 'react';
import { addPageData } from '../../util/add-page-data';

const slug = '/users';
const title = 'Users';

export default addPageData(() => {
  return <div />
}, { slug, title });
