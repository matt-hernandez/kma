import React from 'react';
import { addPageData } from '../../util/add-page-data';

const slug = '/agreements/upcoming';
const title = 'Upcoming Agreements';

export default addPageData(() => {
  return <div />
}, { slug, title });
